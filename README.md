# if-else-javascript
javascript
# Odd Even Number Checker – JavaScript

This project is a simple JavaScript program that checks whether a given number
is **Odd** or **Even** using the `if-else` statement.

## 📌 Features
- Takes user input using `prompt()`
- Checks if the number is odd or even
- Displays result using `alert()`
- Beginner-friendly logic

## 🛠 Technologies Used
- HTML
- JavaScript


